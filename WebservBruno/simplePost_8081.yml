info:
  name: SimpleGet
  type: http

http:
  method: POST
  url: http://localhost:8081/
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  